<%= render 'users/user_info', user: @user ,book: @new_book %>
<table>
  <tr>
    <th>Title</th>
    <th>Body</th>
    <td><%= @book.title %></td>
    <td><%= @book.body %></td>
    <td><%= link_to "Edit", edit_book_path(@book.id) %></td>
    <td><%= button_to "destroy", book_path(@book), method: :delete %>
    </td>
  </tr>
</table>